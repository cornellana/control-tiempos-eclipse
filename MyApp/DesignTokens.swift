/// DesignTokens.swift — App-wide colour and style constants.

import SwiftUI

extension Color {
    /// Brand golden accent (#b8985a).
    static let gold = Color(red: 184 / 255, green: 152 / 255, blue: 90 / 255)
    /// Primary screen background (near-black).
    static let appBackground = Color(red: 0.07, green: 0.07, blue: 0.08)
    /// Card / GroupBox surface.
    static let cardBackground = Color(red: 0.13, green: 0.13, blue: 0.15)
}
